import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.kinzootasty',
  appName: 'kinzootasty',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
